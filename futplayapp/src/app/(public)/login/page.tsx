import Login from "@/components/Login/login";

/**
 * Página de login.
 * Renderiza el componente Login que contiene el formulario de autenticación con Google.
 */
export default function LoginPage() {
  return <Login />;
}
